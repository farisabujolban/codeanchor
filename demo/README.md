# Demo App

This is an intentionally broken demo repo used to showcase `codeanchor` rules.

## Getting started

Install dependencies, then run the app:

```bash
npm install
npm run build
npm run start
```

Deploy to production with `npm run deploy`:

```bash
npm run deploy
```

## More docs

See the full [Setup Guide](./SETUP.md) for environment configuration.
